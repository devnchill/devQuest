#include <stdio.h>
int main(void) {
  int a, b, c;
  float x, y, z;
  printf("|%d| |%d| |%d|", a, b, c);
  printf("|%f| |%f| |%f|", x, y, z);
  return 0;
}

/***************************************/
/*compiling with options like wall wallExtra pendanic gives error while
 * compiling .*/
/*compiling without any extra switch returns random garbage value however some
 * numbers had similarities like showing multiple consecutive zeroes after
 * decimal8 */
/***************************************************************************/
